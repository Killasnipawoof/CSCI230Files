4
5
tlist =[[['me','you','he','she'],'1','2'],['123',78000',age']]

for x in range(len(tlist)):
   g = 4-len(tlist[x])
   if g == 0:
      tlist[x]=tlist[x]
   else:
      tlist[x]=tlist[x] +[' ']*g

def gjPrint(u):
   for x in range(4)
      tlist[u][x] = (' '*(5-len(tlist[u][x]))) + tlist[u][x]
   print(tlist[u])

def tPrint():
   for z in range(len(tlist)):
      gjPrint(z)
