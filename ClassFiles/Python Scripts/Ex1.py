def findServer(serverName)
   for server in cServerDict:
      if server == serverName
         print(cServerDict[serverName]

   return 

def addServer(serverName, mod)
   cServerDict[serverName]=mod
   return
